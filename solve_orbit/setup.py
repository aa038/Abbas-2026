from setuptools import setup, find_packages

setup(
    name='solve_orbit',
    version='0.1',
    description='Orbit Solver',
    author='Asif Abbas',
    packages=find_packages(),
    install_requires=[],
)